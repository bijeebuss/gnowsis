# TLDR Docker Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================
# These values are used by both Docker Compose and the application
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=tldr

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# JWT secret for authentication tokens (change this in production!)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production

# Encryption key for IMAP passwords (64-character hex string)
# Generate with: openssl rand -hex 32
ENCRYPTION_SECRET=your-64-character-hex-string-here

# =============================================================================
# OPENAI API (Required for OCR)
# =============================================================================
# Your OpenAI API key (or compatible endpoint like OpenRouter, Azure OpenAI)
OPENAI_API_KEY=your-openai-api-key-here

# API endpoint (default: OpenAI, can use alternatives like OpenRouter)
OPENAI_API_ENDPOINT=https://api.openai.com/v1

# Model to use for vision/OCR (must support vision)
# Options: gpt-4o, gpt-4o-mini, or compatible vision models
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Environment mode
NODE_ENV=production

# Allowed hosts for the frontend (comma-separated)
# Required when accessing via a custom domain
VITE_ALLOWED_HOSTS=localhost,docs.example.com

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================
# Gotenberg URL for HTML-to-PDF conversion (used for email ingestion)
# Leave as default if using the gotenberg profile
GOTENBURG_URL=http://gotenberg:3000

# Ollama URL for local LLM inference (if using ollama profile)
# OLLAMA_URL=http://ollama:11434
